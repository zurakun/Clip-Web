services:
  - type: web
    name: cliper-app
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PORT
        value: 5000
      - key: SECRET_KEY
        generateValue: true
