services:
  - type: web
    name: cliper-app
    runtime: python
    buildCommand: "chmod +x render-build.sh && ./render-build.sh"
    startCommand: "python app.py"
    envVars:
      - key: PORT
        value: 5000
      - key: SECRET_KEY
        generateValue: true
